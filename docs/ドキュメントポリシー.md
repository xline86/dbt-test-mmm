# ドキュメントポリシー

この文書は、リポジトリ内ドキュメントの責務と更新ルールを定義します。

## 基本方針

- プロジェクトルート `README.md` はプロジェクトの入口として適切な情報を持ち、詳細仕様を抱え込みすぎない。
- 詳細仕様、設計判断、運用手順は `docs/` 配下に責務別のドキュメントとして保存する。
- 同じ情報を`docs/`内の複数文書に重複して書かず、必要な場合はリンクで参照する。
- 実装やSQLのパスを変更した場合は、関連するREADME、docs、Makefileの参照を同時に更新する。
